# Configurations
This directory contains configuration files for the Trend Vigilant Agent Hive project.

## Guidelines
* Each configuration file should be in its own file
* Each configuration file should follow the standard format